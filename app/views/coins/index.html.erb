<p style="color: green"><%= notice %></p>

<h1>Moedas</h1>

<div id="coins">
  <% @coins.each do |coin| %>
    <%= render coin %>
    
      <%= link_to t('links.show'), coin %>
      <p> </p>
    
  <% end %>
</div>

<%= link_to t('links.new'), new_coin_path %>
<p>
<%= link_to t('links.home'), welcome_index_path %>
</p>